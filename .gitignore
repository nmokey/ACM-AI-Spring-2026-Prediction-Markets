# Environment
.env
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Data & model artifacts (too large for git — use shared drive or re-generate)
data/store/
data/features/
models/trained/
logs/
signals/
graphify-out/

# Keep these dirs tracked but empty
!data/store/.gitkeep
!data/features/.gitkeep
!models/trained/.gitkeep
!logs/.gitkeep
!signals/.gitkeep

# NLP cache — generated artifact, changes every 30 min
nlp/sentiment.json

# Notebooks — keep .ipynb but ignore checkpoints and executed copies
.ipynb_checkpoints/
*_executed.ipynb

# OS
.DS_Store
Thumbs.db

uv.lock